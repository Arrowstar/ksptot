function [f,g] = fbeamGVa( x, active )%fbeamGVa - Cantilever beam with N design variables%           from Vanderplaats (1984) Example 5-1, pp. 147-150. % Simulate constraint deletion by returning only active constraints[f,G] = fbeamGV( x );ncon = length(G);if nargin < 2 || isempty(active) || any(isnan(active))   g = G;else   g = repmat(-1,ncon,1); % set g=-1 to simulate not computing   g(active) = G(active); % simulate computing only active constraintsendend